- name: "MX Driver DMA Test"
  description: "DMA test against this driver, after install + reboot, on a full vmm base"
  targets:
    - qemu
  tags:
    - push
    - pr
    - scheduled
  command: |
    cd ~/tests/dma_test && ./test.sh
